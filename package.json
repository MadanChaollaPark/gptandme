{
  "name": "gptandme",
  "version": "1.2.3",
  "private": true,
  "description": "Chrome extension that tracks ChatGPT prompt counts locally.",
  "scripts": {
    "build": "./build.sh",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
